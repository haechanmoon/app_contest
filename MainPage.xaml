<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ShinhanLostAndFound.Models"
             x:Class="ShinhanLostAndFound.MainPage"
             x:DataType="local:MainPage"
             xmlns:local="clr-namespace:ShinhanLostAndFound"
             Title="분실물 목록">

    <Grid RowDefinitions="*, Auto">
        <CollectionView ItemsSource="{Binding LostItems}"
                        Grid.Row="0">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:LostItem">
                    <Frame>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnItemTapped" CommandParameter="{Binding .}"/>
                        </Frame.GestureRecognizers>

                        <Grid ColumnDefinitions="100, *" ColumnSpacing="10">
                            <Image Source="{Binding ImageUrl}" Aspect="AspectFill" HeightRequest="80" WidthRequest="80"/>
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                <Label Text="{Binding Status}" 
                                       TextColor="{StaticResource WhiteColor}"
                                       BackgroundColor="{StaticResource PrimaryColor}" 
                                       Padding="8,2" 
                                       HorizontalOptions="Start"
                                       FontSize="10"/>
                                <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold"/>
                                <Label Text="{Binding Location}" FontSize="14"/>
                                <Label Text="{Binding Date}" TextColor="{StaticResource Gray300}"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
        <Button Grid.Row="1" Text="새 분실물 등록하기" Clicked="AddButton_Clicked"/>
    </Grid>
</ContentPage>